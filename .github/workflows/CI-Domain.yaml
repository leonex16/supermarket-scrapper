name: CI-Domain

on:
  push:
    branches: [ leo-branch ]
  # pull_request:
  #   branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: npm i --force

      - name: Run unit tests
        run: npm run domain:test

      - name: Run build
        run: npm run infra:build
